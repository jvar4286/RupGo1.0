
<%= form_for(payment) do |f| %>
	<% if payment.errors.any? %>
		<<h2><%= pruralize(payment.errors.count, "error")%> avoid it your purchase to success</h2>
	<%end%>

	<%= f.hidden_field :deal_id, value: deal.id %>

<div class="field">
    <%= f.label :Travellers %>
    <%= f.select :quantity, options_for_select(1..12) %>
</div>

<div class="field">
    <%= f.label :Check_in %>
    <%= f.date_select :check_in %>
</div>

<div class="field">
    <%= f.label :Check_Out %>
    <%= f.date_select :check_out %> 
</div>


<div class="actions">
    <%= f.submit %>
</div>

<%end%>
  
