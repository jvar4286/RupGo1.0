<%= form_for(deal) do |f| %>
  <% if deal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deal.errors.count, "error") %> prohibited this deal from being saved:</h2>

      <ul>
      <% deal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :place %>
    <%= f.text_field :place %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :details %>
    <%= f.text_area :details %>
  </div>

  <div class="field">
    <%= f.label :other_details %>
    <%= f.text_area :other_details %>
  </div>

  <div class="field">
    <%= f.label :policies_and_fees %>
    <%= f.text_area :policies_and_fees %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price, step: "any" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
